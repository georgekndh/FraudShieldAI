target: is_fraud
datetime_col: null

random_split:
  test_size: 0.2
  seed: 42

class_weight: balanced

lgbm_params:
  n_estimators: 300
  learning_rate: 0.05
  num_leaves: 31
  max_depth: -1
  subsample: 0.9
  colsample_bytree: 0.9
  random_state: 42